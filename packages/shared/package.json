{
  "name": "@eclipse-timer/shared",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "test": "vitest run --config ../../vitest.config.ts"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
