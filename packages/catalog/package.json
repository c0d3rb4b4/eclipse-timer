{
  "name": "@eclipse-timer/catalog",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@eclipse-timer/shared": "workspace:*"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(add eslint later)\"",
    "test": "echo \"(add tests later)\"",
    "data:filter": "tsx scripts/filter_csv_1900_2100.ts",
    "data:build": "tsx scripts/build_catalog_json.ts",
    "data:all": "pnpm data:filter && pnpm data:build"
  },
  "devDependencies": {
    "csv-parse": "^6.1.0",
    "tsx": "^4.21.0",
    "typescript": "^5.4.0"
  }
}
